# 학식 및 캠퍼스 라이프 구현 - 20

## 작업 개요

신구대학교 학생들이 매일 확인하는 **오늘의 학식 메뉴**와 **캠퍼스 편의시설** 정보를 제공하는 페이지를 구현했습니다. 학생들이 앱을 켜야 할 가장 강력한 이유를 제공합니다.

---

## 주요 구현 사항

### 1. 식당 선택 탭

```tsx
const [selectedCafeteria, setSelectedCafeteria] = useState<CafeteriaType>('south');

<div className="flex gap-3">
  <button
    onClick={() => setSelectedCafeteria('south')}
    className={`flex-1 py-4 px-6 rounded-2xl font-semibold ${
      selectedCafeteria === 'south'
        ? 'bg-primary-500 text-white shadow-lg shadow-primary-500/30'
        : 'bg-white text-gray-700 hover:bg-gray-100'
    }`}
  >
    🏛️ 남관 식당
  </button>
  <button
    onClick={() => setSelectedCafeteria('future')}
    className={`flex-1 py-4 px-6 rounded-2xl font-semibold ${
      selectedCafeteria === 'future'
        ? 'bg-primary-500 text-white shadow-lg shadow-primary-500/30'
        : 'bg-white text-gray-700 hover:bg-gray-100'
    }`}
  >
    🏢 미래창의관 식당
  </button>
</div>
```

**2개 식당:**
- 🏛️ 남관 식당
- 🏢 미래창의관 식당

---

### 2. 혼잡도 배지 (Smart 요소)

```tsx
const getCrowdLevel = () => {
  const hour = new Date().getHours();
  if (hour >= 12 && hour <= 13) return { label: '매우 혼잡', color: 'bg-error text-white' };
  if (hour >= 11 && hour <= 14) return { label: '혼잡', color: 'bg-orange-500 text-white' };
  return { label: '여유', color: 'bg-primary-500 text-white' };
};

<div className="bg-white rounded-2xl p-4 flex items-center justify-between">
  <div className="flex items-center gap-3">
    <Users className="w-5 h-5 text-gray-600" />
    <span className="font-semibold">현재 혼잡도</span>
  </div>
  <span className={`px-4 py-1.5 rounded-full text-sm font-bold ${crowdLevel.color}`}>
    {crowdLevel.label}
  </span>
</div>
```

**혼잡도 로직:**
- 12~13시: 매우 혼잡 (빨강)
- 11~14시: 혼잡 (주황)
- 그 외: 여유 (연두)

---

### 3. 식사 시간 선택

```tsx
const [selectedMealTime, setSelectedMealTime] = useState<MealTime>('lunch');

<div className="flex gap-2 overflow-x-auto">
  {(['breakfast', 'lunch', 'dinner'] as MealTime[]).map((time) => (
    <button
      onClick={() => setSelectedMealTime(time)}
      className={`px-6 py-3 rounded-2xl font-semibold ${
        selectedMealTime === time
          ? 'bg-primary-500 text-white'
          : 'bg-gray-100 text-gray-700 hover:bg-gray-200'
      }`}
    >
      {getMealTimeIcon(time)} {getMealTimeLabel(time)}
    </button>
  ))}
</div>
```

**3개 시간대:**
- 🌅 아침
- ☀️ 점심
- 🌙 저녁

---

### 4. 메뉴 카드

```tsx
interface MenuItem {
  name: string;
  price: number;
  type: 'main' | 'side' | 'soup';
  icon: string;
}

// 남관 식당 점심 메뉴
lunch: [
  { name: '돈까스 + 우동', price: 5500, type: 'main', icon: '🍛' },
  { name: '제육볶음 정식', price: 5000, type: 'main', icon: '🍖' },
  { name: '샐러드바', price: 3500, type: 'side', icon: '🥗' }
]
```

**메뉴 카드 디자인:**
```tsx
<div className="bg-white rounded-3xl p-6 shadow-sm hover-lift">
  <div className="text-5xl mb-3 text-center">{item.icon}</div>
  <h3 className="font-bold text-lg text-center mb-2">
    {item.name}
  </h3>
  {item.price > 0 ? (
    <p className="text-2xl font-bold text-primary-600 text-center">
      {item.price.toLocaleString()}원
    </p>
  ) : (
    <p className="text-sm text-gray-500 text-center">
      기본 제공
    </p>
  )}
  <div className="mt-3 text-center">
    <span className={`px-3 py-1 rounded-full text-xs font-semibold ${
      item.type === 'main' 
        ? 'bg-primary-100 text-primary-700'
        : item.type === 'side'
        ? 'bg-blue-100 text-blue-700'
        : 'bg-orange-100 text-orange-700'
    }`}>
      {item.type === 'main' ? '메인' : item.type === 'side' ? '반찬' : '국'}
    </span>
  </div>
</div>
```

**카드 특징:**
- 큰 이모지: `text-5xl`
- 메뉴명: `font-bold text-lg`
- 가격: `text-2xl text-primary-600`
- 타입 배지: 메인(연두), 반찬(파랑), 국(주황)
- Hover: `hover-lift`
- Framer Motion: 순차 등장

---

### 5. 식단 데이터

#### 남관 식당
```typescript
const southMeals: Meal = {
  breakfast: [
    { name: '김치찌개 + 밥', price: 3500, type: 'main', icon: '🍲' },
    { name: '계란후라이', price: 0, type: 'side', icon: '🍳' },
    { name: '김치', price: 0, type: 'side', icon: '🥬' }
  ],
  lunch: [
    { name: '돈까스 + 우동', price: 5500, type: 'main', icon: '🍛' },
    { name: '제육볶음 정식', price: 5000, type: 'main', icon: '🍖' },
    { name: '샐러드바', price: 3500, type: 'side', icon: '🥗' }
  ],
  dinner: [
    { name: '김치찌개 정식', price: 4500, type: 'main', icon: '🍲' },
    { name: '불고기 덮밥', price: 5000, type: 'main', icon: '🍚' },
    { name: '된장국', price: 0, type: 'soup', icon: '🥣' }
  ]
};
```

#### 미래창의관 식당
```typescript
const futureMeals: Meal = {
  breakfast: [
    { name: '토스트 세트', price: 3000, type: 'main', icon: '🍞' },
    { name: '샌드위치', price: 3500, type: 'main', icon: '🥪' }
  ],
  lunch: [
    { name: '떡볶이 + 튀김', price: 4500, type: 'main', icon: '🍢' },
    { name: '비빔밥', price: 5000, type: 'main', icon: '🍚' },
    { name: '라면', price: 3000, type: 'main', icon: '🍜' }
  ],
  dinner: [
    { name: '치킨 덮밥', price: 5500, type: 'main', icon: '🍗' },
    { name: '김밥 세트', price: 4000, type: 'main', icon: '🍱' }
  ]
};
```

---

### 6. 편의시설 안내

```tsx
interface Facility {
  name: string;
  location: string;
  hours: string;
  isOpen: boolean;
  icon: any;
}

const facilities: Facility[] = [
  { name: '교내 서점', location: 'A관 1층', hours: '09:00 - 18:00', isOpen: true, icon: BookOpen },
  { name: 'GS25 편의점', location: 'B관 지하', hours: '08:00 - 22:00', isOpen: true, icon: Store },
  { name: '카페 미래', location: '미래창의관 1층', hours: '09:00 - 19:00', isOpen: true, icon: Coffee },
  { name: '우체국', location: 'A관 1층', hours: '09:00 - 17:00', isOpen: false, icon: Mail }
];
```

**시설 카드 디자인:**
```tsx
<div className="p-5 bg-gray-50 rounded-2xl flex items-start gap-4">
  <div className="w-12 h-12 rounded-full bg-primary-100 flex items-center justify-center">
    <Icon className="w-6 h-6 text-primary-600" />
  </div>
  <div className="flex-grow">
    <div className="flex items-center gap-2 mb-1">
      <h3 className="font-semibold">{facility.name}</h3>
      <span className={`px-2 py-0.5 rounded-full text-xs font-bold ${
        facility.isOpen 
          ? 'bg-primary-500 text-white'
          : 'bg-gray-400 text-white'
      }`}>
        {facility.isOpen ? '영업중' : '종료'}
      </span>
    </div>
    <p className="text-sm text-gray-600">
      📍 {facility.location}
    </p>
    <div className="flex items-center gap-1 text-sm text-gray-500">
      <Clock className="w-4 h-4" />
      {facility.hours}
    </div>
  </div>
</div>
```

**4개 시설:**
- 📚 교내 서점
- 🏪 GS25 편의점
- ☕ 카페 미래
- 📮 우체국

---

## 사용자 플로우

```
1. 학식 페이지 접속
   ↓
2. 식당 선택 (남관 / 미래창의관)
   ↓
3. 혼잡도 확인 (여유/혼잡/매우 혼잡)
   ↓
4. 식사 시간 선택 (아침/점심/저녁)
   ↓
5. 메뉴 카드 확인 (음식 아이콘 + 가격)
   ↓
6. 스크롤하여 편의시설 확인
   ↓
7. 영업중/종료 상태 파악
```

---

## 다음 단계 제안

### 홈 위젯 (우선순위 높음)
Home.tsx 상단에 오늘의 점심 요약:
```tsx
<div className="bg-white rounded-2xl p-4 mb-4">
  <div className="flex items-center justify-between">
    <div>
      <p className="text-sm text-gray-600">오늘의 점심</p>
      <p className="font-bold text-lg">돈까스 + 우동</p>
    </div>
    <button className="text-primary-600">
      자세히 →
    </button>
  </div>
</div>
```

---

## 요약

### 구현 완료
1. ✅ Cafeteria.tsx 생성
2. ✅ 2개 식당 탭 (남관/미래창의관)
3. ✅ 혼잡도 배지 (시간대별)
4. ✅ 3개 식사 시간 (아침/점심/저녁)
5. ✅ 메뉴 카드 (이모지 + 가격)
6. ✅ 편의시설 4개 (영업 상태)
7. ✅ 라우팅 추가 (/cafeteria)

### 디자인 원칙
- **#a2c54e 테마**: 활성 탭, 가격, 영업중
- **큰 이모지**: 직관적 음식 표현
- **rounded-3xl**: 부드러운 카드

### 파일 생성
**신규:**
- `src/pages/Cafeteria.tsx`

**수정:**
- `src/App.tsx` (라우트 추가)

---

**작성**: 2026-01-13  
**다음 작업**: 홈 대시보드 통합 - 21
